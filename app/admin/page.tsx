import Link from "next/link";import { PageShell } from "@/components/page-shell";
export default function AdminHome(){return <PageShell><section className="shell section"><p className="eyebrow">ADMIN AREA</p><h1 className="section-title serif">Boutique management</h1><div className="glass" style={{borderRadius:14,padding:26,maxWidth:800}}><h2 className="serif">Secure administration</h2><p style={{color:'var(--muted)',lineHeight:1.7}}>This route is reserved for authenticated administrators. Add the Auth.js adapter and assign an ADMIN role in the Prisma user record before exposing management screens in production.</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}>{['Products','Orders','Customers','Categories','Blog','Settings'].map(x=><Link key={x} href={`/admin/${x.toLowerCase()}`} className="btn btn-ghost">{x}</Link>)}</div></div></section></PageShell>}
