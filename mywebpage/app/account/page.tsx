import Link from "next/link";import { PageShell } from "@/components/page-shell";
export default function Account(){return <PageShell><section className="shell section" style={{maxWidth:720}}><p className="eyebrow">YOUR ACCOUNT</p><h1 className="section-title serif">Welcome back</h1><div className="glass" style={{padding:25,borderRadius:14}}><p style={{color:'var(--muted)'}}>Account sign-in is enabled when the Auth.js database adapter is configured. You can still browse and add items to your local cart.</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><Link href="/account/orders" className="btn btn-primary">View orders</Link><Link href="/contact" className="btn btn-ghost">Need help?</Link></div></div></section></PageShell>}
